# RNN, LSTM, GRU
Exemplary code implementations demonstrating the use of Recurrent Neural Networks (RNN), Long Short-Term Memory (LSTM), and Gated Recurrent Unit (GRU) models for energy consumption prediction (regression task).
